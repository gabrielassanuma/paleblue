<%= current_user.tokens %>

<%= simple_form_for [@creator, @transaction] do |f| %>
  <%= f.input :tk_amount %>
  <%= f.association :token,
                    collection: current_user.tk_balances,
                    label_method: :token %>
  <%= f.button :submit %>
<% end %>
