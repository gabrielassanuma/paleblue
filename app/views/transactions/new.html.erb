<%= simple_form_for [@creator, @transaction] do |f| %>
  <%= f.input :tk_amount %>
  <%= f.association :token,
                    collection: @tokens,
                    label_method: :nickname %>
  <%= f.button :submit %>
<% end %>
